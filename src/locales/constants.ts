import type { LocaleOptions } from '@/locales/types'

export const CUSTOM_HEADER_KEY = 'x-web-language'

export const DEFAULT_OPTIONS: LocaleOptions = {
	defaultValue: 'en',
	supportValues: ['en'],
}
